#include "matrix.h"

char x_data[] = { 1, 0, 4, 7 };

struct matrix x = {
    x_data, // Initialize the data field
    2,           // Initialize the width field
    2            // Initialize the height field
};
